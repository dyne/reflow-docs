% Latex rendering headers
\setlength{\parindent}{1.25em}
\setlength{\parskip}{.2em}
\usepackage{etoolbox}

\AtBeginEnvironment{quote}{\parskip 1em}

\usepackage[margin=2.5cm]{geometry}

% Need to compile pandoc with `--listings` for this to work - submitting a PR to writedown for this
% Or we can make this a writedown option.
% uncomment this to customise the way it looks
%% \lstset{
%%     basicstyle=\ttfamily,
%% %    numbers=left,
%%     numberstyle=\footnotesize,
%%     stepnumber=2,
%%     numbersep=5pt,
%%     backgroundcolor=\color{black!10},
%%     showspaces=false,
%%     showstringspaces=false,
%%     showtabs=false,
%%     tabsize=2,
%%     captionpos=b,
%%     breaklines=true,
%%     breakatwhitespace=true,
%%     breakautoindent=true,
%%     linewidth=\textwidth
%% }
