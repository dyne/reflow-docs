# Test directory for writedown

This dir contains some simple test files and a makefile to use writedown to compile them.

You can use it to verify that things are working and experiment with new features.
